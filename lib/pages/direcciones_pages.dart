import 'package:flutter/material.dart';

class Direccionespges extends StatelessWidget {
  const Direccionespges({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('hola2'),
    );
  }
}
